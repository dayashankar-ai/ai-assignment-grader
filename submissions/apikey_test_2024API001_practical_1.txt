STUDENT_NAME: API Key Test Student
STUDENT_ID: 2024API001
STUDENT_EMAIL: apitest@example.com
BATCH: 2024
PRACTICAL: 1

# Temperature Monitoring System - API Key Test

Testing the grading system with updated API credentials.

## Implementation

```python
import datetime
import statistics

class TemperatureMonitor:
    def __init__(self):
        self.readings = []
        self.timestamps = []
        print("Temperature monitor initialized!")
    
    def add_reading(self, temp, timestamp=None):
        if timestamp is None:
            timestamp = datetime.datetime.now()
        self.readings.append(temp)
        self.timestamps.append(timestamp)
    
    def get_average(self):
        if not self.readings:
            return 0.0
        return statistics.mean(self.readings)
    
    def get_max(self):
        return max(self.readings) if self.readings else 0.0
    
    def get_min(self):
        return min(self.readings) if self.readings else 0.0
    
    def get_report(self):
        return {
            'average': self.get_average(),
            'max': self.get_max(),
            'min': self.get_min(),
            'total': len(self.readings)
        }

if __name__ == "__main__":
    monitor = TemperatureMonitor()
    
    monitor.add_reading(22.5)
    monitor.add_reading(23.1)
    monitor.add_reading(21.8)
    monitor.add_reading(24.2)
    
    report = monitor.get_report()
    print(f"Average: {report['average']:.2f}°C")
    print(f"Max: {report['max']:.2f}°C")
    print(f"Min: {report['min']:.2f}°C")
```

## Learning Notes

I learned how to use the statistics module for calculating mean values. Had to debug empty list edge cases.
